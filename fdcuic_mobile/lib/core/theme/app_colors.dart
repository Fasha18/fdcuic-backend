import 'package:flutter/material.dart';

class AppColors {
  // Primary & Secondary
  static const Color primary = Color(0xFF0A1D56);
  static const Color secondary = Color(0xFF4D8DFF);
  
  // Background
  static const Color background = Color(0xFFF5F8FF);
  
  // Premium Glass / Transparent Silver
  static final Color premiumGlass = Colors.white.withOpacity(0.15);
  
  // Text Colors
  static const Color textPrimary = Color(0xFF1E293B); // Dark slate
  static const Color textSecondary = Color(0xFF64748B); // Slate
  
  // Surface
  static const Color surface = Colors.white;
  static const Color surfaceElevated = Color(0xFFF1F5F9);
  
  // Status
  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
}
